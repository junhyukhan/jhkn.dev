---
type: Decision
title: <Topic>
description: <One sentence stating what was decided. This is the record's machine API — the
  generated decisions index shows this line and nothing else, so it must be enough for an agent
  to recognise that this record applies. "Storage layer chosen" is useless; "SQLite persistence
  retired 2026-07-30, seen.json replaced it" is the bar.>
status: draft
tags: []
generated: { by: human:junhyukhan, at: <YYYY-MM-DDTHH:MM:SSZ> }
---

# <Topic>

**Status:** SPEC | in progress | done | superseded (<YYYY-MM-DD>)

<!-- Frontmatter is OKF v0.2 (https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md),
     used as-is with no local extensions — see repos/docs/decisions/cross-repo-retrieval.md.

     status:  draft | stable | deprecated   (§5.4; absent means stable)
              draft      = proposed, not settled
              stable     = decided and current
              deprecated = no longer current. If a newer record replaced it, say so in prose
                           with a link — OKF links are untyped by design (§6.1), so supersession
                           is a sentence, not a field.
     generated.by: `human:<id>`, `<producer>/<version>`, or `process:<id>` (§7). The `human:`
              prefix is what marks content as hand-authored, so use it when you wrote it.
     The prose **Status:** line above stays — it carries the detail the enum can't. -->

## Why — the ask (verbatim)

> **Verbatim (<YYYY-MM-DD>):** "<paste the exact words — informal wording and all; the raw
> phrasing captures intent and 취향 better than a tidy restatement>"

<!-- Stack more verbatim quotes as the decision evolves. Mid-build steers and taste calls count
     too — a one-line course-correction carries intent, so quote it, don't paraphrase. -->

## Discussion

<!-- Analysis / options considered / the decision and why / open questions. -->

<!-- NEWEST FIRST. When this topic evolves, the new amendment goes directly below this heading,
     above the older ones — not appended to the bottom. A reader must reach the current state
     without reading the history. This rule predates the frontmatter and was not followed:
     cross-repo-retrieval.md put its newest entry at line ~870 of 877. -->
