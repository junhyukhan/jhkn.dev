---
const { tag, size = "sm" } = Astro.props;

const config = {
    sm: "px-2 py-1 text-sm",
    base: "px-3 py-2 text-base",
};

const classes = config[size as keyof typeof config] || config.sm;
---

<a
    href={`/tags/${tag}`}
    class={`inline-block font-mono text-gray-600 bg-gray-100 rounded hover:bg-gray-200 transition-colors no-underline ${classes}`}
>
    #{tag}
</a>
